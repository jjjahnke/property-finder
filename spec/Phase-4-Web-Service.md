# Spec: Phase 4 - Frontend and User Interface

**Status:** `pending`

This document outlines the tasks required to set up the web service and UI.

## TODO List

- [ ] Set up the **Web Service** (Next.js).
- [ ] Create a basic UI with an input field for a `parcel_id`.
- [ ] Connect the UI to the Query Service to display the results for the entered parcel.
