apiVersion: v1
kind: Service
metadata:
  namespace: propfinder
  name: qdrant-service
spec:
  selector:
    app: qdrant
  ports:
    - protocol: TCP
      port: 6333
      targetPort: 6333
  type: ClusterIP
